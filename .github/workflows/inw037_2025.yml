name: run inw037_2025

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */8 * * *"   # รันทุก 6 ชม.

jobs:
  run-python:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install gdown
        run: pip install gdown

      - name: Python script
        env:
          FILE_URL: ${{ secrets.INW037_2025 }}   
        run: |
          set +x  
          gdown "$FILE_URL" -O script.py

      - name: Install dependencies
        run: |
          pip install -r requirements.txt || true

      - name: Run Script
        run: python script.py
